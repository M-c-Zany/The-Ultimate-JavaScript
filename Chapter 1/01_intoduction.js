// Display a note to view files from the REPL to access the source code
console.log("Note: View Files from this Repl to Access the Source Code");

// Display a message indicating the purpose of the program
console.log("This program adds 1, 2, and 54");

// Function to add three numbers and return the result
function addThreeNumbers(a, b, c) {
    return a + b + c;
}

// Call the addThreeNumbers function with specific values
let result = addThreeNumbers(1, 2, 54);

// Display the result of the addition
console.log("Result:", result);
